# 2026-09-17 公开源码同步

本次从已完成的早苗部署源码按白名单导出；没有重启服务、发送 QQ 消息或修改 Minecraft。

- 聊天记录回溯、引用/合并转发材料读取、受限长上下文与请求超时管理。
- 默认先给短总结，再折叠完整时间线；支持只要短版，以及复用同用户/群/权限内上一条总结，正文上限 240 字。
- 明确提及时才进入相应 AI 路由，保留命令身份校验、确认和去重；通用文件写入工具已停用，本地运维工具保持只读。
- 媒体处理拆为下载/转换与模型分析两层；补充 Silk 适配源码，Node、ffmpeg、silk-wasm 由部署者自行配置，不随源码分发。
- 包含卡顿取证、Spark 归档、周报、启动提示、贴纸选择与模组详情等已完成模块；性能历史趋势及完整性能分级不在本次实现范围。

公开版适配：群/机器人身份和 LLBot 路径通过配置传入；聊天回溯排除已配置的机器人自身消息。修正视觉模型费用口径：未配置可靠费率的非 DeepSeek provider 显示无法计算，不套用 DeepSeek 价格。这些公开副本修正未回写生产。

验证：164 项离线回归通过，0 失败、0 错误、0 跳过；按模块隔离进程运行，阻断外部 socket 连接，覆盖 14 项短总结行为、路由/权限/写入停用、媒体、贴纸、运维和本地化。测试使用合成身份、临时目录和模拟请求；未在真实 QQ 群发送测试消息，未连接 Minecraft，也未重新进行真实模型和完整生产验收。

完整 Minecraft 官方语言文件、聊天材料、贴纸、私有配置与依赖二进制不分发。无客户端语言包时仅使用内置回退文案，不声称全部采用最新官方译名。`nast_ops_sync.py` 只作为旧接口的显式可选兼容模块保留，不表示当前部署启用 NAST。

运行公开版回归：`python scripts/test-offline.py`。失败日志和结果保存在忽略的 `build/offline-tests/`。
